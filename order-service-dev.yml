eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true # This is also good practice for Docker/containerized environments

order:
  max: 10
  currency: $
